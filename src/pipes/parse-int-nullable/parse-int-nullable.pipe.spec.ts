import { ParseIntNullablePipe } from './parse-int-nullable.pipe';

describe('ParseIntNullablePipe', () => {
  it('should be defined', () => {
    expect(new ParseIntNullablePipe()).toBeDefined();
  });
});
