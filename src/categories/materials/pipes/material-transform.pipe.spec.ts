import { MaterialTransformPipe } from './material-transform.pipe';

describe('MaterialTransformPipe', () => {
  it('should be defined', () => {
    expect(new MaterialTransformPipe()).toBeDefined();
  });
});
